# 🚀 Plataforma de Upskilling/Reskilling 2030+

API RESTful para gestão de usuários, trilhas de aprendizagem, competências e matrículas — foco em Upskilling/Reskilling para carreiras do futuro.

---

## 🔧 Versões e dependências obrigatórias
- **Java:** 21  
- **Spring Boot:** 3.2.x  
- **Principais frameworks / dependências:** Spring Boot Web, Spring Data JPA, Spring Validation, Spring Security (opcional), H2 (dev), MySQL (prod), Maven, Swagger/OpenAPI (documentação)

---

## 🚀 Comandos principais

Instalar dependências:
mvn clean install

Iniciar a aplicação (com Maven instalado):
mvn spring-boot:run

Iniciar a aplicação (com wrapper):
./mvnw spring-boot:run

---

## 🌐 Porta padrão da API
A API sobe por padrão em:
http://localhost:8080

Swagger UI:
http://localhost:8080/swagger-ui/index.html

H2 Console (dev):
http://localhost:8080/h2-console
JDBC URL: jdbc:h2:mem:testdb
Usuário: sa
Senha: (vazia)

---

## 🔗 Endpoints e exemplos de requisições (URLs + JSON de exemplo)

### 1) Criar usuário  
POST /usuarios  
Payload JSON:
{
  "nome": "Ana Souza",
  "email": "ana.souza@example.com",
  "areaAtuacao": "Tecnologia",
  "nivelCarreira": "Junior"
}

### 2) Listar usuários  
GET /usuarios

### 3) Buscar usuário por ID  
GET /usuarios/{id}

### 4) Atualizar usuário  
PUT /usuarios/{id}  
Payload JSON (exemplo):
{
  "nome": "Ana S. Atualizada",
  "email": "ana.souza@example.com",
  "areaAtuacao": "Data Science",
  "nivelCarreira": "Pleno"
}

### 5) Deletar usuário  
DELETE /usuarios/{id}

### 6) Criar trilha  
POST /trilhas  
Payload JSON:
{
  "nome": "Fundamentos de IA",
  "descricao": "Introdução à IA e modelos generativos",
  "nivel": "INICIANTE",
  "cargaHoraria": 40,
  "focoPrincipal": "IA"
}

### 7) Listar trilhas  
GET /trilhas

### 8) Criar competência  
POST /competencias  
Payload JSON:
{
  "nome": "Machine Learning",
  "categoria": "Tecnologia",
  "descricao": "Modelos supervisionados e não-supervisionados"
}

### 9) Relacionar trilha ↔ competência (associação)
POST /trilhas/{idTrilha}/competencias/{idCompetencia}

### 10) Criar matrícula  
POST /matriculas  
Payload JSON:
{
  "usuarioId": 1,
  "trilhaId": 2,
  "status": "ATIVA"
}

### 11) Listar matrículas  
GET /matriculas

---

## 🗄 Modelo SQL Sugerido (MySQL-like; para H2 trocar AUTO_INCREMENT por IDENTITY se necessário)

```sql
-- Tabela de usuários
CREATE TABLE usuarios (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    nome VARCHAR(100) NOT NULL,
    email VARCHAR(150) NOT NULL UNIQUE,
    area_atuacao VARCHAR(100),
    nivel_carreira VARCHAR(50),
    data_cadastro DATE NOT NULL
);

-- Tabela de trilhas
CREATE TABLE trilhas (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    nome VARCHAR(150) NOT NULL,
    descricao TEXT,
    nivel VARCHAR(50) NOT NULL,
    carga_horaria INT NOT NULL,
    foco_principal VARCHAR(100)
);

-- Tabela de competências
CREATE TABLE competencias (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    nome VARCHAR(100) NOT NULL,
    categoria VARCHAR(100),
    descricao TEXT
);

-- Relação N:N entre trilhas e competências
CREATE TABLE trilha_competencia (
    trilha_id BIGINT NOT NULL,
    competencia_id BIGINT NOT NULL,
    PRIMARY KEY (trilha_id, competencia_id),
    CONSTRAINT fk_trilha_competencia_trilha FOREIGN KEY (trilha_id) REFERENCES trilhas (id),
    CONSTRAINT fk_trilha_competencia_competencia FOREIGN KEY (competencia_id) REFERENCES competencias (id)
);

-- Matrículas
CREATE TABLE matriculas (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    usuario_id BIGINT NOT NULL,
    trilha_id BIGINT NOT NULL,
    data_inscricao DATE NOT NULL,
    status VARCHAR(50) NOT NULL,
    CONSTRAINT fk_matricula_usuario FOREIGN KEY (usuario_id) REFERENCES usuarios (id),
    CONSTRAINT fk_matricula_trilha FOREIGN KEY (trilha_id) REFERENCES trilhas (id)
);
```

---

## 📌 Observações e boas práticas
- Para H2 em memória troque AUTO_INCREMENT por IDENTITY ou GENERATED BY DEFAULT AS IDENTITY caso ocorra erro.  
- Recomendo adicionar um arquivo .gitignore (ignorando /target, .idea, .mvn/wrapper, *.iml, application-prod.properties).  
- Para migrações e versionamento de schema considere usar Flyway ou Liquibase.  
- Configure variáveis de ambiente para credenciais do banco (spring.datasource.*) em produção.  

---

## 📄 Licença
Projeto sugerido como template (MIT ou escolha da sua organização).
